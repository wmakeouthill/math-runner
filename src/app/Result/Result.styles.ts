import type { CSSProperties } from 'react';
import { PALETTE } from '@/theme/palette';
import { arcadeFace, arcadePanel, ghostButton, hardShadow, pixelFace, primaryButton } from '@/theme/arcade';

export const styles = {
  backdrop: {
    position: 'absolute',
    inset: 0,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    padding: '1rem',
    background: `${PALETTE.night}d9`,
    zIndex: 5,
  },
  card: {
    ...arcadePanel,
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    gap: '0.55rem',
    width: 'min(380px, 92vw)',
    padding: '1.3rem 1.4rem',
    textAlign: 'center',
    borderColor: PALETTE.cyan,
    boxShadow: hardShadow(PALETTE.navy, 8),
    animation: 'subir 320ms ease-out both',
  },
  banner: {
    ...arcadeFace,
    fontSize: '0.78rem',
    lineHeight: 1.5,
    color: PALETTE.cyan,
    textShadow: hardShadow(PALETTE.navy, 3),
  },
  levelName: {
    ...pixelFace,
    fontSize: '0.85rem',
    color: PALETTE.mute,
  },
  stars: {
    display: 'flex',
    gap: '0.3rem',
    margin: '0.4rem 0',
    fontSize: '2.4rem',
    lineHeight: 1,
  },
  star: { color: PALETTE.gold },
  starOff: { color: PALETTE.steel },
  lines: {
    ...pixelFace,
    display: 'flex',
    flexDirection: 'column',
    gap: '0.2rem',
    fontSize: '0.85rem',
    color: PALETTE.ink,
  },
  missing: {
    ...pixelFace,
    fontSize: '0.78rem',
    color: PALETTE.faint,
    maxWidth: '36ch',
  },
  actions: {
    display: 'flex',
    gap: '0.7rem',
    width: '100%',
    marginTop: '0.8rem',
  },
  primary: {
    ...primaryButton,
    flex: 1,
    fontSize: '0.55rem',
    padding: '0.8rem 0.5rem',
  },
  secondary: {
    ...ghostButton,
    flex: 1,
    fontSize: '0.55rem',
    padding: '0.8rem 0.5rem',
  },
  confetti: {
    position: 'absolute',
    inset: 0,
    overflow: 'hidden',
    pointerEvents: 'none',
  },
  flake: {
    position: 'absolute',
    top: '-12px',
    width: '9px',
    height: '14px',
    borderRadius: 0,
  },
} satisfies Record<string, CSSProperties>;
